<Window x:Class="Quiz_playable.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quiz_playable"
        mc:Ignorable="d"
        Title="Solve Quiz" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Czas: " FontSize="16" FontWeight="Bold"/>
            <TextBlock x:Name="TimerText" Text="00:00" FontSize="16" FontWeight="Bold" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <ComboBox x:Name="CmbQuizzes" Width="150" Height="30" Margin="5" DisplayMemberPath="Name" SelectedValuePath="Id" />
            <Button x:Name="BtnRozpocznij" Content="Rozpocznij" Width="100" Height="30" Margin="5" Click="BtnRozpocznij_Click" IsEnabled="False"/>
            <Button x:Name="BtnZakoncz" Content="Zakończ" Width="100" Height="30" Margin="5" IsEnabled="False" Click="BtnZakoncz_Click"/>
            <Button x:Name="BtnShowAnswers" Content="Poprawne odp." Width="100" Height="30" Margin="5" IsEnabled="False" Click="BtnShowAnswers_Click"/>
        </StackPanel>

        <TextBlock x:Name="ScoreText" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="16" FontWeight="Bold" Foreground="Green" Margin="10,0,0,0"/>

        <Border x:Name="QuestionArea" Grid.Row="2" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,10" Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="QuestionTextBlock" Grid.Row="0" FontSize="18" TextWrapping="Wrap" Margin="0,0,0,10" FontWeight="SemiBold"/>

                <ListBox x:Name="AnswersListBox" Grid.Row="1" BorderBrush="Transparent" SelectionMode="Single">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <RadioButton Content="{Binding Text}" 
                                         GroupName="Answers"
                                         IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Mode=TwoWay}"
                                         Margin="0,5" FontSize="14"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <StackPanel x:Name="NavigationPanel" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10" Visibility="Collapsed">
            <Button x:Name="BtnPrevious" Content="Poprzednie" Width="100" Height="30" Margin="5" Click="BtnPrevious_Click"/>
            <TextBlock x:Name="ProgressText" Text="1 / 1" VerticalAlignment="Center" Margin="10,0"/>
            <Button x:Name="BtnNext" Content="Następne" Width="100" Height="30" Margin="5" Click="BtnNext_Click"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="4" HorizontalAlignment="Center" Foreground="Gray" Margin="0,5"/>
    </Grid>
</Window>
