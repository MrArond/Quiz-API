<Window x:Class="Quiz.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quiz"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Quizzes Column -->
        <StackPanel Grid.Column="0" Margin="5">
            <TextBlock Text="Quizzes" FontWeight="Bold" Margin="0,0,0,5"/>
            <ListBox x:Name="QuizzesListBox" DisplayMemberPath="Name" SelectionChanged="QuizzesListBox_SelectionChanged" Height="200"/>
            <TextBlock Text="New Quiz Name:" Margin="0,10,0,0"/>
            <TextBox x:Name="NewQuizNameTextBox"/>
            <TextBlock Text="Description:" Margin="0,5,0,0"/>
            <TextBox x:Name="NewQuizDescTextBox"/>
            <Button Content="Add Quiz" Margin="0,5,0,0" Click="AddQuizButton_Click"/>
        </StackPanel>

        <!-- Questions Column -->
        <StackPanel Grid.Column="1" Margin="5">
            <TextBlock Text="Questions" FontWeight="Bold" Margin="0,0,0,5"/>
            <ListBox x:Name="QuestionsListBox" DisplayMemberPath="Text" SelectionChanged="QuestionsListBox_SelectionChanged" Height="200"/>
            <TextBlock Text="Question Text:" Margin="0,10,0,0"/>
            <TextBox x:Name="QuestionTextBox"/>
            <Button Content="Add Question" Margin="0,5,0,0" Click="AddQuestionButton_Click"/>
            <Button Content="Update Question" Margin="0,5,0,0" Click="UpdateQuestionButton_Click"/>
            <Button Content="Remove Question" Margin="0,5,0,0" Click="RemoveQuestionButton_Click"/>
        </StackPanel>

        <!-- Answers Column -->
        <StackPanel Grid.Column="2" Margin="5">
            <TextBlock Text="Answers (Add to selected question)" FontWeight="Bold" Margin="0,0,0,5"/>
            <ListBox x:Name="AnswersListBox" Height="200" SelectionChanged="AnswersListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Text}" Width="150" />
                            <TextBlock Text="{Binding IsCorrect}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Text="Answer Text:" Margin="0,10,0,0"/>
            <TextBox x:Name="AnswerTextBox"/>
            <CheckBox x:Name="IsCorrectCheckBox" Content="Is Correct" Margin="0,5,0,0"/>
            <Button Content="Add Answer" Margin="0,5,0,0" Click="AddAnswerButton_Click"/>
            <Button Content="Update Answer" Margin="0,5,0,0" Click="UpdateAnswerButton_Click"/>
            <Button Content="Remove Answer" Margin="0,5,0,0" Click="RemoveAnswerButton_Click"/>
        </StackPanel>

    </Grid>
</Window>
